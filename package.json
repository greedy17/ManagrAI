{
  "scripts": {
    "postinstall": "cd client && npm install",
    "heroku-postbuild": "cd client && npm run heroku-postbuild"
  },
  "engines": {
    "node": "14.x.x",
    "npm": "6.x.x"
  },
  "cacheDirectories": [
    "client/node_modules",
    ".cache/Cypress"
  ],
  "dependencies": {
    "@thinknimble/togglecheckbox": "^0.1.4"
  }
}
